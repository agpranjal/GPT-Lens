{
  "name": "gpt-lens",
  "version": "0.1.0",
  "private": true,
  "description": "GPT Lens — chat app with selection-driven follow-up actions powered by OpenRouter",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install && npm --prefix extension install",
    "build:extension": "npm --prefix extension run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
